{
  "name": "quetrex-base",
  "version": "2.5.8",
  "private": true,
  "description": "Quetrex — the Claude Code plugin engine (agents, skills, commands, hooks). Distributed as a plugin, not an npm package; this package.json exists only to run the repo's dev/verify scripts.",
  "scripts": {
    "check:js": "node --check test/plugin.test.js",
    "check:sh": "bash test/lib/check-sh.sh",
    "check:json": "bash test/lib/check-json.sh",
    "test": "bash test/run-all.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
