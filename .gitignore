.DS_Store
*.swp
*.swo
*~
node_modules/
*.db

# Local secrets — never commit
.env
.env.*
!.env.example

# Runner and auto-pilot run-state files — local only, never commit
.claude/runner-*.json
.claude/autopilot-*.json
